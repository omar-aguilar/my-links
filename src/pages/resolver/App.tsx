import MainContext from '@/pages/_shared/MainContext';
import Resolver from './components/Resolver';

const App = () => {
  return (
    <MainContext>
      <Resolver />
    </MainContext>
  );
};

export default App;
